language: node_js

node_js:
  - "node"

addons:
    apt:
        packages:
            - sshpass

branches:
  only:
    - master

install:
  - npm install --silent

script:
  - npm run build
  #  - sed -ie 's/<\/body>/<script src="\/ads.js?<?php echo uniqid() ?>"><\/script><\/body>/g' ./build/index.html
  #  - sed -ie 's/__aid__/<?php echo uniqid() ?>/g' ./build/index.html
  #  - mv ./build/index.html ./build/index.php
  #  - rm -f ./build/index.htmle

after_success:
  - sshpass -p $SSH_PASSWORD ssh $SSH_USER@$SSH_HOST pwd